#include <stdio.h>

int g[5][5],v[5];

void dfs(int i){
    v[i]=1;
    printf("%d ",i);
    for(int j=0;j<5;j++)
        if(g[i][j]&&!v[j])
            dfs(j);
}

int main(){
    g[0][1]=g[1][2]=g[2][3]=1;
    dfs(0);
}
